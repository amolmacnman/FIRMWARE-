# Central MISRA / Cppcheck deviation list for the Smart Wagon firmware.
# Each line is a documented, justified deviation (assessor evidence).
# Format:  <check-id>:<file>:<line>   # justification
# Examples (uncomment/adjust once triaged):
#
# misra-c2012-15.5:src/ec200.c   # early return on AT error keeps the driver readable; reviewed
# misra-c2012-21.6               # printk/snprintf used for logging/payloads; controlled use
#
# Prefer fixing over suppressing. Use inline "// cppcheck-suppress <id>" for
# one-off, location-specific deviations so the rationale lives next to the code.
qZwe5&el